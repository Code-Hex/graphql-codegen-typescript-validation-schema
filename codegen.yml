overwrite: true
schema: "./test.graphql"
generates:
  gen/types.ts:
    plugins:
      - typescript
  gen/schemas.ts:
    plugins:
      - ./dist/index.js:
          schema: yup
          importFrom: ./types