overwrite: true
schema: "./test.graphql"
generates:
  gen/types.ts:
    plugins:
      - typescript
  gen/schemas.ts:
    plugins:
      - ./dist/main/index.js:
          schema: yup
          importFrom: ./types